'use strict';


let rightClickedItem = null;

if (document.body) {
  document.body.addEventListener((getElementById("about"), (e) => {
    rightClickedItem = e
  });
  document.body.addEventListener('click', () => {
    rightClickedItem = null;
  });
}
if (document.body) {
  document.body.addEventListener((getElementById("projects"), (e) => {
    rightClickedItem = e
  });
  document.body.addEventListener('click', () => {
    rightClickedItem = null;
  });
}
if (document.body) {
  document.body.addEventListener((getElementById("contact"), (e) => {
    rightClickedItem = e
  });
  document.body.addEventListener('click', () => {
    rightClickedItem = null;
  });
}
